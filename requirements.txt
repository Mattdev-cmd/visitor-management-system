# Requirements for the Visitor Management & Emotion Recognition System.
# Tested on Raspberry Pi 5 (2 GB) with Python 3.11+.

# --- Web Framework ---
flask==3.1.*

# --- Database ---
# SQLite is built-in; no extra driver required.

# --- Image / CV ---
opencv-python-headless>=4.9
Pillow>=10.0

# --- Face Recognition ---
face-recognition==1.3.*
dlib-bin>=19.24

# --- Emotion Recognition (optional) ---
# Lightweight TFLite runtime (much smaller than full TensorFlow on Pi)
# tflite-runtime>=2.14; platform_machine == "aarch64"
# For development on x86 (requires Python <= 3.12):
# tensorflow>=2.14; platform_machine != "aarch64"

# --- Utilities ---
numpy>=1.26
